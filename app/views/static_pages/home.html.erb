<div class="page-header col-md-6">
  <h1>Leaderboard <small> </small></h1>
</div>
<table class="table table-hover table-bordered col-md-6">
  <tr>
    <th>Username</th>
    <th>Score</th>
  </tr>
  <% @top10.each do |u| %>
  <tr>
    <td><%= u.first %></td>
    <td><%= u.last %></td>
  </tr>
  <% end %>
</table>